# AI Context: defaultapp

## Description
A new Flutter project.

## Framework vs Project Division
- **Framework (`lib/core`)**: Contains the reusable UI components, networking, themes, and base classes.
- **Project (`lib/features`)**: Contains specific implementations leveraging the framework.

## How to Help
When acting as an AI assistant in this repository:
1. Always read `.ai/framework.json` to understand what utilities already exist.
2. Always read `.ai/CONVENTIONS.md` and strictly follow the custom widget and structure guidelines.
3. Use `AI_GRAPH.json` and `patterns.json` to safely navigate architectural patterns.
4. Update these configurations by running `dart tools/setup_ai.dart` after significant changes.
